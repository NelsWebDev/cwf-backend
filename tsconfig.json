// tsconfig.json (main one for dev and testing)
{
  "extends": "./tsconfig.build.json",
  "compilerOptions": {
    "types": ["vitest/globals", "node"]
  },
  "include": [
    "src/**/*.ts",
    "src/__tests__/**/*.ts"
  ],
  "exclude": ["node_modules", "dist"]
}
